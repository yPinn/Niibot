# Docker Compose — PostgreSQL credentials and in-container DATABASE_URL.

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Containers use the service name, not localhost
DOCKER_DATABASE_URL=postgresql://USER:PASSWORD@postgres:5432/DB

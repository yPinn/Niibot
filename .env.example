# GENERATED by scripts/gen_env.py — do not edit; edit env.registry.toml
# Root .env — read by Docker Compose for variable substitution.
# Copy to .env and fill in values. Never commit it.

# ── Database ──────────────────────────────────────────────────────────────────
# In-container URL — host is the compose service name, not localhost.
DOCKER_DATABASE_URL=
POSTGRES_DB=
POSTGRES_PASSWORD=
POSTGRES_USER=

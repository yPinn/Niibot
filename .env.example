# GENERATED by scripts/env/gen.py — do not edit; edit env.registry.toml
# Root env template — copied to .env.dev, .env.stg, or .env.prod.
# Create a runtime set with: npm run nb -- env init <dev|stg|prod>
# Never commit runtime env files.

# ── Database ──────────────────────────────────────────────────────────────────
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
# 容器 host 固定為 postgres，不可使用 localhost。
DOCKER_DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Docker Compose variables — PostgreSQL credentials and DATABASE_URL override.
# Local dev: copy to .env and fill in. Production: injected from GitHub Secrets.

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Containers cannot reach localhost — must reference the Docker service name.
DOCKER_DATABASE_URL=postgresql://USER:PASSWORD@postgres:5432/DB

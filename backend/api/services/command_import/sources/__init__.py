"""Per-platform adapters that turn a remote command list into an ImportPreview.

Import the adapters from their own modules — the exception types live there too
and a facade that exports only half of what callers need is worse than none.
"""

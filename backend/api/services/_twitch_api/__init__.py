"""TwitchAPIClient mixins, split out of ``services.twitch_api`` to keep files small.

The public entry point stays ``services.twitch_api.TwitchAPIClient`` — this
sub-package holds the per-domain mixins it is composed from.
"""

# GENERATED by scripts/env/gen.py — do not edit; edit env.registry.toml
# API service overrides.
# Create a runtime set with: npm run nb -- env init <dev|stg|prod>
# Never commit runtime env files.

# ── App ───────────────────────────────────────────────────────────────────────
# 供 OAuth callback 與付款 webhook 使用。
API_URL=

# ── Auth ──────────────────────────────────────────────────────────────────────
# 產生：python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=
# JWT_ALGORITHM=HS256
# JWT_EXPIRE_DAYS=7
# 部署必填且不可任意更換。產生：python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
PAYMENT_ENCRYPTION_KEY=

# ── Service ───────────────────────────────────────────────────────────────────
# PORT=8000

# ── Integrations ──────────────────────────────────────────────────────────────
# 需要 read:contents 權限。
# RELEASES_GITHUB_TOKEN=
# prod 與 nonprod 各一組；dev 使用 staging 值。
# NIGHTBOT_CLIENT_ID=
# NIGHTBOT_CLIENT_SECRET=
# 僅啟用 Webhook Events 時設定；dev 使用 staging 值。
# DISCORD_PUBLIC_KEY=

# ── Dev ───────────────────────────────────────────────────────────────────────
# 供 OAuth callback 與付款 webhook 使用。
# API_URL=http://localhost:8000

# GENERATED by scripts/gen_env.py — do not edit; edit env.registry.toml
# API service. App credentials → shared.env.
# Copy to .env and fill in values. Never commit it.

# ── App ───────────────────────────────────────────────────────────────────────
API_URL=

# ── Auth ──────────────────────────────────────────────────────────────────────
# JWT_ALGORITHM=HS256
# JWT_EXPIRE_DAYS=7
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
PAYMENT_ENCRYPTION_KEY=

# ── Integrations ──────────────────────────────────────────────────────────────
# From Developer Portal → General Information → Public Key.
# DISCORD_PUBLIC_KEY=
# From nightbot.tv/account/applications; one app per environment.
# NIGHTBOT_CLIENT_ID=
# NIGHTBOT_CLIENT_SECRET=
# From github.com/settings/tokens; needs read:contents scope.
# RELEASES_GITHUB_TOKEN=

# ── Dev ───────────────────────────────────────────────────────────────────────
# API_URL=http://localhost:8000

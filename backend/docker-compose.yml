version: '3.8'

services:
  twitch-bot:
    build: .
    container_name: niibot-twitch
    restart: unless-stopped
    ports:
      - "4343:4343"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge

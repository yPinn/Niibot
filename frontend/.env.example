# GENERATED by scripts/env/gen.py — do not edit; edit env.registry.toml
# Frontend (Vite) — dev uses .env.dev; deploy via platform env.
# Create a runtime set with: npm run nb -- env init <dev|stg|prod>
# Never commit runtime env files.

# ── App ───────────────────────────────────────────────────────────────────────
# 用於過濾 Bot 自身訊息。
VITE_BOT_USERNAME=niibot_
VITE_DISCORD_COMMUNITY_URL=
# VITE_DISCORD_BOT_INVITE_URL=
# VITE_SUPPORT_ECPAY_URL=

# ── Deployment ────────────────────────────────────────────────────────────────
# production 鎖定 WIP；staging 或空值不鎖。
# VITE_ENVIRONMENT=production

# ── Dev ───────────────────────────────────────────────────────────────────────
# 正式部署不設定。
# VITE_API_URL=http://localhost:8000

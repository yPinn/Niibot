# Ignore files that use # for shell-style comments — not Markdown headings.

.secretlintignore
.gitignore
.prettierignore
.eslintignore
.dockerignore
frontend/.prettierignore
frontend/.dockerignore
backend/.dockerignore
frontend/public/_headers

# Pack data files — long CJK paragraphs / frontmatter-first (no H1).
# markdownlint-cli does not support per-directory config overrides.
backend/data/packs/

# Partly generated by scripts/env/gen.py (env:begin..env:end block) —
# compared verbatim by `npm run env:check`, so formatters must leave it alone.
docs/guides/environment.md
